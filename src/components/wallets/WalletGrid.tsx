import type { Wallet } from '../../types/finance';
import { WalletAccountCard } from './WalletAccountCard';
export interface WalletGridProps { wallets: Wallet[]; openMenuId: string | null; onToggleMenu: (id: string) => void; onView: (wallet: Wallet) => void; onEdit: (wallet: Wallet) => void; onSetLimit: (wallet: Wallet) => void; onDelete: (wallet: Wallet) => void; onCreate?: () => void; }
export function WalletGrid(props: WalletGridProps) { return <section aria-label="Wallet accounts" className="grid grid-cols-1 md:grid-cols-2 xl:grid-cols-3 gap-4 sm:gap-5">{props.wallets.map(wallet => <WalletAccountCard key={wallet.id} wallet={wallet} menuOpen={props.openMenuId === wallet.id} onToggleMenu={() => props.onToggleMenu(wallet.id)} onView={() => props.onView(wallet)} onEdit={() => props.onEdit(wallet)} onSetLimit={() => props.onSetLimit(wallet)} onDelete={() => props.onDelete(wallet)} />)}{props.wallets.length === 0 && <div className="rounded-2xl border border-dashed border-border bg-card p-8 text-center md:col-span-2 xl:col-span-3"><span className="mx-auto flex h-10 w-10 items-center justify-center rounded-full bg-surface text-secondary"><i className="bi bi-wallet2" aria-hidden="true" /></span><p className="mt-3 text-sm font-semibold text-primary">No wallets yet</p><p className="mt-1 text-xs text-secondary">Add a wallet to start tracking its persisted ledger balance.</p>{props.onCreate && <button type="button" onClick={props.onCreate} className="mt-4 rounded-full bg-dark px-3.5 py-2 text-xs font-semibold text-white active:scale-[0.98]">Add Wallet</button>}</div>}</section>; }
