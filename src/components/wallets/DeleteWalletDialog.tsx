import type { Wallet } from '../../types/finance';
import { Button } from '../ui/Button';
import { Modal } from '../ui/Modal';
export function DeleteWalletDialog({ wallet, onCancel, onConfirm }: { wallet?: Wallet | null; onCancel: () => void; onConfirm: () => void }) { return <Modal isOpen={Boolean(wallet)} onClose={onCancel} title={`Delete ${wallet ? `“${wallet.name}”` : 'wallet'}?`} description="This only removes the local mock wallet in the current prototype." maxWidth="sm" footer={<><Button variant="outline" size="sm" onClick={onCancel}>Cancel</Button><Button variant="primary" size="sm" className="bg-danger hover:bg-danger/90" onClick={onConfirm}>Delete Wallet</Button></>}><p className="text-sm leading-relaxed text-secondary">This action does not affect any real account or payment source.</p></Modal>; }
