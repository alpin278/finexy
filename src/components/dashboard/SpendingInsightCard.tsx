import { Card } from '../ui/Card';
import type { OverviewCategorySpending } from '../../lib/overview';
import { formatWalletAmount } from '../../lib/overview';
import type { WalletCurrencyCode } from '../../types/finance';
export interface SpendingInsightCardProps { categories: OverviewCategorySpending[]; currency: WalletCurrencyCode; className?: string; }
export function SpendingInsightCard({ categories, currency, className }: SpendingInsightCardProps) { return <Card className={className}><div className="flex items-start justify-between gap-3"><div className="flex items-start gap-2.5"><div className="flex h-8 w-8 items-center justify-center rounded-xl bg-accent/10 text-accent"><i className="bi bi-tags" aria-hidden="true" /></div><div><h3 className="text-sm font-bold text-primary">Spending by category</h3><p className="mt-0.5 text-xs text-secondary">Completed {currency} expenses in the selected month</p></div></div><i className="bi bi-arrow-up-right text-secondary" aria-hidden="true" /></div>{categories.length ? <div className="mt-5 space-y-4">{categories.map((category) => <div key={category.id}><div className="flex items-center justify-between gap-3 text-xs"><span className="font-medium text-primary">{category.label}</span><span className="font-semibold text-secondary">{formatWalletAmount(category.amount, currency)}</span></div><div className="mt-2 h-2 overflow-hidden rounded-full bg-border/60" role="progressbar" aria-label={`${category.label} share of spending`} aria-valuemin={0} aria-valuemax={100} aria-valuenow={Math.round(category.percentage)}><div className="h-full rounded-full" style={{ width: `${category.percentage}%`, backgroundColor: category.color }} /></div><p className="mt-1 text-[11px] text-secondary">{category.percentage.toFixed(1)}% of selected-period spending</p></div>)}</div> : <p className="py-7 text-xs text-secondary">No completed {currency} expenses in this period.</p>}<p className="mt-4 border-t border-border pt-3 text-[11px] leading-relaxed text-secondary">Transfers and pending, canceled, or archived transactions are excluded.</p></Card>; }
export default SpendingInsightCard;
