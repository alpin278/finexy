import { Card } from '../ui/Card';
import { WalletCard } from './WalletCard';
import type { Wallet } from '../../types/finance';
import { cn } from '../../lib/utils';
export interface WalletListProps { wallets: Wallet[]; onWalletAction?: (walletId: string) => void; onAddWallet?: () => void; className?: string; }
export function WalletList({ wallets, onWalletAction, onAddWallet, className }: WalletListProps) { return <Card className={cn('flex flex-col justify-between p-5 sm:p-6', className)}><div className="flex items-center justify-between border-b border-border/60 pb-3"><div className="flex items-center gap-2"><h3 className="text-sm font-bold text-primary">Wallets</h3><span className="text-xs font-medium text-secondary">| Total {wallets.length} wallets</span></div><button type="button" onClick={onAddWallet} className="inline-flex cursor-pointer items-center gap-1 text-xs font-semibold text-accent transition-colors hover:text-accent-hover"><i className="bi bi-plus-lg" aria-hidden="true" /><span>Add</span></button></div><div className="flex flex-col gap-2.5 pt-3.5">{wallets.length ? wallets.slice(0, 4).map((wallet) => <WalletCard key={wallet.id} wallet={wallet} onActionClick={onWalletAction} />) : <p className="py-4 text-xs text-secondary">No wallets yet.</p>}</div><button type="button" onClick={() => onWalletAction?.('all')} className="mt-3 inline-flex items-center gap-1 self-end text-xs font-semibold text-accent hover:text-accent-hover">View all wallets <i className="bi bi-arrow-right" aria-hidden="true" /></button></Card>; }
export default WalletList;
