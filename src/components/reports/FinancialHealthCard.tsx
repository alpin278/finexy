import type { ReportBudgetContext } from '../../lib/reports'; import { Card } from '../ui/Card';
export function FinancialHealthCard({ savingsRate, net, budgetContext }: { savingsRate: number; net: number; budgetContext: ReportBudgetContext | null }) { const cashFlow = net >= 0 ? 'Positive cash flow' : 'Negative cash flow'; return <Card padding="none" className="h-full overflow-hidden p-4 sm:p-6"><div className="flex items-start justify-between gap-4"><div><p className="text-[10px] font-bold uppercase tracking-[0.12em] text-accent">Real indicators</p><h2 className="mt-1 text-lg font-bold tracking-tight text-primary">Financial Health</h2><p className="mt-1 text-xs text-secondary">Transparent metrics from your selected ledger period.</p></div><span className="flex h-9 w-9 items-center justify-center rounded-[11px] bg-accent/10 text-accent"><i className="bi bi-heart-pulse" aria-hidden="true" /></span></div><div className="mt-5 grid gap-3 sm:grid-cols-2"><div className="rounded-[14px] bg-surface p-3"><p className="text-[10px] font-semibold uppercase text-secondary">Savings rate</p><p className="mt-1 text-xl font-bold text-primary">{savingsRate.toFixed(1)}%</p></div><div className="rounded-[14px] bg-surface p-3"><p className="text-[10px] font-semibold uppercase text-secondary">Cash flow status</p><p className={net >= 0 ? 'mt-1 text-sm font-bold text-success' : 'mt-1 text-sm font-bold text-danger'}>{cashFlow}</p></div>{budgetContext ? <><div className="rounded-[14px] bg-surface p-3"><p className="text-[10px] font-semibold uppercase text-secondary">Over-budget categories</p><p className="mt-1 text-xl font-bold text-primary">{budgetContext.overBudgetCount}</p></div><div className="rounded-[14px] bg-surface p-3"><p className="text-[10px] font-semibold uppercase text-secondary">Budget utilization</p><p className="mt-1 text-xl font-bold text-primary">{budgetContext.utilization === null ? 'No budgets' : `${budgetContext.utilization.toFixed(1)}%`}</p></div></> : <p className="text-xs leading-5 text-secondary sm:col-span-2">Budget context is shown only for a complete calendar month to avoid misleading comparisons.</p>}</div></Card>; }
