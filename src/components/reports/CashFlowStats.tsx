import type { ReportStat } from '../../types/reports';
import { Card } from '../ui/Card';
export function CashFlowStats({ stats }: { stats: ReportStat[] }) { return <Card padding="none" className="grid grid-cols-1 divide-y divide-border/70 bg-surface/45 sm:grid-cols-3 sm:divide-x sm:divide-y-0">{stats.map((item) => <div key={item.label} className="flex items-center gap-3 px-4 py-3.5 sm:px-5"><span className="flex h-8 w-8 shrink-0 items-center justify-center rounded-full bg-white text-secondary shadow-sm"><i className={`bi ${item.icon}`} aria-hidden="true" /></span><div className="min-w-0"><p className="truncate text-[10px] font-semibold uppercase tracking-[0.08em] text-secondary">{item.label}</p><p className="mt-1 truncate text-sm font-bold text-primary">{item.value}</p></div></div>)}</Card>; }
