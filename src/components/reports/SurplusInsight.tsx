import type { WalletCurrencyCode } from '../../types/finance'; import { Card } from '../ui/Card'; import { formatMoney } from './reportUtils';
export function SurplusInsight({ net, currency, overBudgetCount }: { net: number; currency: WalletCurrencyCode; overBudgetCount: number | null }) { const message = net >= 0 ? `Net cash flow for this period is +${formatMoney(net, currency)}.` : `Net cash flow for this period is -${formatMoney(Math.abs(net), currency)}.`; return <Card padding="none" className="h-full overflow-hidden border-dark bg-dark p-4 text-white sm:p-6"><div className="flex h-full flex-col justify-between gap-7"><div><div className="flex items-center gap-2 text-accent"><i className="bi bi-lightbulb" aria-hidden="true" /><p className="text-[10px] font-bold uppercase tracking-[0.12em]">Period insight</p></div><h2 className="mt-5 text-xl font-bold tracking-tight">A clear view of this period.</h2><p className="mt-2 text-xs leading-5 text-white/65">{message}{overBudgetCount !== null ? ` ${overBudgetCount} categories are currently over budget.` : ''}</p></div><p className="text-[10px] leading-4 text-white/45">Informational only. Finexy does not provide investment recommendations or execute actions here.</p></div></Card>; }
