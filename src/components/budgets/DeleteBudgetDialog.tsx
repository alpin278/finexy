import type { Budget } from '../../types/finance';
import { Button } from '../ui/Button';
import { Modal } from '../ui/Modal';
export function DeleteBudgetDialog({ budget, onCancel, onConfirm }: { budget?: Budget | null; onCancel: () => void; onConfirm: () => void }) { return <Modal isOpen={Boolean(budget)} onClose={onCancel} title={`Archive budget for ${budget ? `“${budget.categoryName}”` : 'category'}?`} description="This archives the persisted budget configuration." maxWidth="sm" footer={<><Button variant="outline" size="sm" onClick={onCancel}>Cancel</Button><Button variant="primary" size="sm" className="bg-danger hover:bg-danger/90" onClick={onConfirm}>Archive Budget</Button></>}><p className="text-sm leading-relaxed text-secondary">Transactions and categories are not affected. Financial history remains available.</p></Modal>; }
