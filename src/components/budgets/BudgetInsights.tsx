import { AlertCircle, ArrowUpRight, CheckCircle2 } from 'lucide-react';
import type { Budget } from '../../types/finance';
import { Card } from '../ui/Card';
import { money } from './budgetUtils';
export function BudgetInsights({ budgets }: { budgets: Budget[] }) { const over=budgets.filter(b=>b.status==='over_budget'); const near=budgets.filter(b=>b.status==='near_limit'); return <Card padding="md" aria-label="Budget insights"><div className="flex items-center gap-2"><CheckCircle2 className="w-4 h-4 text-accent" aria-hidden="true"/><h2 className="text-sm font-bold text-primary">Budget insights</h2></div><div className="mt-4 grid grid-cols-1 sm:grid-cols-2 gap-3">{over.length ? <div className="rounded-xl border border-danger/20 bg-danger/5 p-3"><div className="flex items-start gap-2"><AlertCircle className="mt-0.5 h-4 w-4 shrink-0 text-danger" aria-hidden="true"/><p className="text-xs leading-relaxed text-primary"><span className="font-semibold">{over[0].categoryName}</span> exceeded its budget by <span className="font-semibold text-danger">{money(over[0].spent-over[0].monthlyLimit)}</span>.</p></div></div> : <div className="rounded-xl border border-success/20 bg-success/5 p-3 text-xs text-primary">All categories are within their monthly budgets.</div>}{near.length ? <div className="rounded-xl border border-warning/30 bg-warning/10 p-3"><div className="flex items-start gap-2"><ArrowUpRight className="mt-0.5 h-4 w-4 shrink-0 text-[#9E8314]" aria-hidden="true"/><p className="text-xs leading-relaxed text-primary"><span className="font-semibold">{near[0].categoryName}</span> is nearing its monthly limit.</p></div></div> : null}</div></Card>; }
