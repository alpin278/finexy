import type { BudgetStatus } from '../../types/finance';

export type BudgetFilter = 'all' | BudgetStatus;
const tabs: { value: BudgetFilter; label: string }[] = [{ value: 'all', label: 'All Budgets' }, { value: 'on_track', label: 'On Track' }, { value: 'near_limit', label: 'Near Limit' }, { value: 'over_budget', label: 'Over Budget' }];
export function BudgetTabs({ active, onChange, counts }: { active: BudgetFilter; onChange: (value: BudgetFilter) => void; counts: Record<BudgetFilter, number> }) { return <div role="tablist" aria-label="Budget status filters" className="flex items-center gap-1.5 overflow-x-auto pb-1 scrollbar-none">{tabs.map(tab => <button key={tab.value} type="button" role="tab" aria-selected={active===tab.value} onClick={()=>onChange(tab.value)} className={active===tab.value ? 'shrink-0 rounded-full bg-dark px-3.5 py-2 text-xs font-semibold text-white cursor-pointer' : 'shrink-0 rounded-full border border-border bg-white px-3.5 py-2 text-xs font-semibold text-secondary hover:bg-surface hover:text-primary cursor-pointer'}>{tab.label} <span className={active===tab.value ? 'ml-1 text-white/70' : 'ml-1 text-secondary/70'}>{counts[tab.value]}</span></button>)}</div>; }
